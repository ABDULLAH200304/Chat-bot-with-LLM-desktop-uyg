<Window x:Class="ChatBotApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatBotApp"
        mc:Ignorable="d"
        Title="AI Chat Bot" Height="600" Width="800" Background="#F5F5F7">
    <Window.Resources>
        <local:RoleToAlignmentConverter x:Key="RoleToAlignmentConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / API Key Input -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="OpenAI API Key:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <PasswordBox x:Name="ApiKeyBox" Width="400" VerticalAlignment="Center" Padding="5"/>
            <Button Content="Set Key" Click="SetKey_Click" Margin="10,0,0,0" Padding="10,5" Background="#007AFF" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <!-- Chat History -->
        <ListBox x:Name="ChatListBox" Grid.Row="1" Background="White" BorderThickness="1" BorderBrush="#E5E5E5" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="5" Padding="10" CornerRadius="10" MaxWidth="600" HorizontalAlignment="{Binding Role, Converter={StaticResource RoleToAlignmentConverter}}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Role}" Value="User">
                                        <Setter Property="Background" Value="#007AFF"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                        <Setter Property="Background" Value="#E9E9EB"/>
                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Role}" Value="User">
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                                <Setter Property="Foreground" Value="Black"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm'}" FontSize="10" Margin="0,5,0,0" Opacity="0.6">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Role}" Value="User">
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                                <Setter Property="Foreground" Value="Black"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Input Area -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputTextBox" Grid.Column="0" Padding="10" FontSize="14" BorderThickness="1" BorderBrush="#E5E5E5" KeyDown="InputTextBox_KeyDown" AcceptsReturn="False"/>
            <Button Content="Send" Grid.Column="1" Click="Send_Click" Margin="10,0,0,0" Padding="20,10" Background="#007AFF" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Window>
